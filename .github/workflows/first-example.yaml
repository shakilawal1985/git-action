name: My First Workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My First Github Action Job"
            
            - name: List Files
              run: ls
            
            - name: Read the files
              run: cat readme.md
            
        